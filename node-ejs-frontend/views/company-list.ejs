<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('includes/head', { title: 'All Company' }) %>
  <link rel="stylesheet" href="/styles/business.css">
</head>

<body>
  <%- include('includes/header') %>
  <main id="all-posts">
    <h1>Company list</h1>
    <% if (!posts || posts.length === 0) { %>
      <p>No posts found - maybe start creating some?</p>
      <a class="btn" href="/create-company">Create a new post</a>
    <% } else { %>
      <ol id="posts-list">
        <% for (const post of posts) { %>
          <li>
            <%- include('includes/post-item', { post: post }) %>
          </li>
        <% } %>
      </ol>
    <% } %>
  </main> 
</body>

</html>